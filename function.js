const addNumber = (x, y) => {
    const sum = x + y;
    
    return sum;
};

const multiplyNumbers = (x, y) => {
    const product = x + y;

    return product;
};

export {addNumber, multiplyNumbers};